# Copy to .env (gitignored) and fill in. The key stays server-side; the Godot
# client never sees it and only ever talks to http://127.0.0.1:8000.
ANTHROPIC_API_KEY=sk-ant-...

# Optional model overrides.
# ZIJ_WORLD_MODEL=claude-sonnet-5
# ZIJ_DIALOGUE_MODEL=claude-haiku-4-5
# ZIJ_CRITIC_MODEL=claude-sonnet-5
